name: Publish container
on:
  push: {tags: ["v*"]}
  workflow_dispatch:
permissions: {contents: read, packages: write}
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with: {registry: ghcr.io, username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}"}
      - uses: docker/metadata-action@v5
        id: meta
        with: {images: "ghcr.io/${{ github.repository_owner }}/production-ml-platform"}
      - uses: docker/build-push-action@v6
        with: {context: ., file: docker/Dockerfile.api, push: true, build-args: "GIT_SHA=${{ github.sha }}", tags: "${{ steps.meta.outputs.tags }}", labels: "${{ steps.meta.outputs.labels }}"}
